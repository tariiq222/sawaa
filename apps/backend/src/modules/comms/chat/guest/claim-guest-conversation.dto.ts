/**
 * Claiming derives client identity from ClientSessionGuard and the guest
 * credential from the HttpOnly cookie. It deliberately has no body fields.
 */
export class ClaimGuestConversationDto {}
