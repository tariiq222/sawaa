import { PlatformSettingsModule } from './platform-settings.module';

describe('PlatformSettingsModule', () => {
  it('should be defined', () => {
    expect(PlatformSettingsModule).toBeDefined();
  });
});
