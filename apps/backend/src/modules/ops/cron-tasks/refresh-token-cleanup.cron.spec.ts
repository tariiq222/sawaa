import { RefreshTokenCleanupCron } from './refresh-token-cleanup.cron';

describe('RefreshTokenCleanupCron', () => {
  it('should be defined', () => {
    expect(RefreshTokenCleanupCron).toBeDefined();
  });
});
