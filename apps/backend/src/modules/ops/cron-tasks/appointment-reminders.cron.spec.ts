import { AppointmentRemindersCron } from './appointment-reminders.cron';

describe('AppointmentRemindersCron', () => {
  it('should be defined', () => {
    expect(AppointmentRemindersCron).toBeDefined();
  });
});
