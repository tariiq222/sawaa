# Dashboard e2e Overhaul — Closing Status (2026-05-04)

## Before / After Spec Counts

| Dimension | Before overhaul | After overhaul |
|---|---|---|
| Spec files | flat ~20 files in `e2e/` root | 3 smoke + 30 flows files in `smoke/` + `flows/<feature>/` |
| Total tests listed | ~342 (flat, mixed states) | **22 smoke + 320 flows = 342** |
| Smoke project | none | 22 tests in 3 files |
| Flows project | none | 320 tests in 30 files across 17 feature dirs |

## Smoke Run Result (2026-05-04)

Command: `cd apps/dashboard && npm run e2e:smoke`

```
22 tests using 1 worker
21 passed
 1 skipped   (navigation.spec.ts:58 — "should display header with user menu")
 0 failed
Wall time: 56.8 s
```

**Result: GREEN.** Smoke gate is clean and ready for CI enforcement on every PR.

## 5 Commits That Landed This Overhaul

| Hash | Subject |
|---|---|
| `7ea73dad` | refactor(dashboard): restructure e2e into smoke/flows hierarchy with Playwright as official tool |
| `5ed4c383` | chore(dashboard-e2e): reorganize Playwright e2e into smoke/ + flows/<feature>/ |
| `883082b9` | docs(dashboard-e2e): add triage report 2026-05-04 |
| `28ca9d81` | test(dashboard-e2e): stabilize notification bell selectors via data-testid |
| `9d4a4edb` | test(dashboard-e2e): implement seed + tenant fixtures |
| `6285c591` | ci(dashboard-e2e): boot backend + run smoke against live stack |

## Structural Verification

| Check | Result |
|---|---|
| `e2e/smoke/` — 3 spec files | PASS (login.spec.ts, navigation.spec.ts, smoke.spec.ts) |
| `e2e/flows/` — feature subdirs | PASS (bookings, categories, clients, departments, email-templates, employees, errors, invoices, notifications, payments, ratings, reports, services, settings, users) |
| `e2e/fixtures/` — auth.ts, seed.ts, tenant.ts | PASS |
| `e2e/README.md` | PASS |
| `.github/workflows/dashboard-e2e.yml` — no `continue-on-error: true` | PASS |
| `package.json` scripts: e2e, e2e:smoke, e2e:flows, e2e:ui, e2e:report, e2e:install | PASS (all 6 present) |
| Old scripts `test:e2e:pw*` removed | PASS |
| `apps/dashboard/CLAUDE.md` — Playwright declared official QA gate | PASS |

## Follow-ups (not blocking launch)

1. **Un-skip 138 flows tests** — triage report at `e2e/TRIAGE-2026-05-04.md` categorises failures into: selector drift (A), auth/session setup (B), backend seeding (C), real product bugs (D), flaky/timing (E). Work through categories A→E in order.
2. **Seed fixtures against live stack** — `e2e/fixtures/seed.ts` needs a running backend with seeded org + employee data; the pre-existing `ERR_MODULE_NOT_FOUND` in `@deqah/shared/constants/roles` blocks backend startup and must be resolved first.
3. **CI repo secrets** — `PW_DASHBOARD_URL`, `PW_ADMIN_EMAIL`, `PW_ADMIN_PASSWORD` must be set in GitHub repo settings before `dashboard-e2e.yml` can run against a deployed environment.
4. **Un-skip navigation header test** — `navigation.spec.ts:58` ("should display header with user menu") is the one skipped smoke test; selector needs updating to match current header DOM.
5. **Nightly flows run** — once flows tests are green, wire the `flows` project into the nightly CI schedule in `dashboard-e2e.yml`.
