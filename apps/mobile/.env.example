EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1

# hCaptcha placeholder — leave empty in dev (falls back to 'dev-bypass' which
# passes the Noop verifier). Post-launch: integrate react-native-hcaptcha and
# set to the verified token instead.
EXPO_PUBLIC_HCAPTCHA_TOKEN=

# --- Sentry / GlitchTip error tracking ---
EXPO_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
