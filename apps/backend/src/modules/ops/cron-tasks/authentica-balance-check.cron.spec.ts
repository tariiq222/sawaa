import { AuthenticaBalanceCheckCron } from './authentica-balance-check.cron';

describe('AuthenticaBalanceCheckCron', () => {
  it('should be defined', () => {
    expect(AuthenticaBalanceCheckCron).toBeDefined();
  });
});
