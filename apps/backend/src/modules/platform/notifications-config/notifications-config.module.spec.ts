import { NotificationsConfigModule } from './notifications-config.module';

describe('NotificationsConfigModule', () => {
  it('should be defined', () => {
    expect(NotificationsConfigModule).toBeDefined();
  });
});
